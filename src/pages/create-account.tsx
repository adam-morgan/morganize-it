import { BasePage } from "@/components";
import { CreateAccountContainer } from "@/features";

const CreateAccountPage = () => {
  return (
    <BasePage>
      <CreateAccountContainer />
    </BasePage>
  );
};

export default CreateAccountPage;
