import { BasePage } from "@/components";
import { LoginContainer } from "@/features";

const LoginPage = () => {
  return (
    <BasePage>
      <LoginContainer />
    </BasePage>
  );
};

export default LoginPage;
